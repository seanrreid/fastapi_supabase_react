const Protected = () => {
    return (
        <>
            <h1>Protected Route</h1>
            <p>You should only see this if you are logged in.</p>
        </>
    );
};

export default Protected;